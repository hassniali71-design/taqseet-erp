# خطة التنفيذ الكاملة Phases 1->9

مطابق لـ §131 في Spec. كل Phase فرعية = تعديل محدد -> build/lint/tsc نظيفين -> اختبار Playwright فعلي -> لقطة شاشة لو فيها قيمة -> تحديث STATE -> commit + push.

مفيش وقفات للسؤال الا عند Blocker حقيقي (قرار مالي حساس Spec تقول "يحتاج تأكيد" - يتسجل كاعداد قابل للتفعيل بدل ما يخترع).

تعريف "جاهز للتسليم": كل الشاشات شغالة ومختبرة بمتصفح حقيقي، Build/Lint/TS نظيفين، التوثيق يعكس الواقع، ولسه Mock وليس Production-Ready بمعايير §134 حتى يتم ربط Supabase الحقيقي - يذكر صراحة في كل تسليم.
